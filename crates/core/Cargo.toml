[package]
name = "zkpow-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[features]
host = []
profiling = []
copy = []

[dependencies]
sp1-zkvm = { workspace = true }
thiserror = { version = "2.0", default-features = false }
