[package]
name = "bitcoin-header-chain-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
rkyv = { version = "0.7.43", default-features = false, features = ["alloc", "size_64"] }
sp1-zkvm = { workspace = true }
