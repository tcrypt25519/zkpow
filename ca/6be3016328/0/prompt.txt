Continue the DB-backed simplification pass on `dry-refactor`. Extend the new `load_header_batch_witness` and `HeaderRecord::public_claim` approach anywhere the host still loads or reconstructs more DB state than it actually needs, but do not re-open already-fixed execute-only or retarget behavior. After the cleanup, run the highest-signal host checks for divergence and the retarget boundary cases, then commit, push, and update the PR if the slice is solid.